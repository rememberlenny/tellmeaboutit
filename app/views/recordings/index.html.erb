<h1>Recordings for <%= link_to @story.name, @story %></h1>
<% @recordings.each do |recording| %>
<div class="row">
  <div class="column">
    <h4><%= link_to recording.source, [@story, recording] %></h4>
    <time>Created <%= recording.created_at %></time>
  </div>
</div>
<% end %>
