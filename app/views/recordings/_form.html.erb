<%= simple_form_for(@recording) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :url %>
    <%= f.input :length %>
    <%= f.input :transcript %>
    <%= f.input :twilio_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
